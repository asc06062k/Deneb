# Phase 3 — บทที่ 6 review request (สำหรับ Codex CLI)

คุณคือผู้รีวิวอิสระของ eBook สอนสร้าง Dual-Line Variance Chart ด้วย Deneb บทที่ 1–5 `PASS` แล้ว บทนี้คือ **บทที่ 6 พื้นที่ Variance และ Connector**: [`manuscript/chapter-06.md`](../../manuscript/chapter-06.md) มี 4 Step ปฏิบัติ (มาตรฐาน 12 หัวข้อ) Step 1–3 ใช้ไฟล์เต็มใน `specs/steps/CH06-S01..S03-*.vl.json` (สร้างจาก final spec ด้วย generator ผ่านชุดทดสอบ Phase 2 แล้ว) Step 4 ไม่มีไฟล์ใหม่ เป็นการสลับ `Business_Type` เป็น `Lower is Good` ที่ Power Query

Source of truth: `PROJECT_PLAN.md` (บทที่ 6, มาตรฐาน Step, กฎภาพ, ข้อจำกัด M-11), `specs/steps/*`, `specs/dual-line-variance-final.vl.json`, `specs/DualLine_PlotData_PowerQuery.pq`, `review/PHASE1_DESIGN_PLAN.md`, `manuscript/chapter-03.md` (หัวข้อ 3.8 condition), `manuscript/chapter-04.md` (Row_Type/Segment_ID/Run_Sign), `manuscript/chapter-05.md`

ภาพ: `images/chapter-06/` 6 ภาพ
- 6-1, 6-2, 6-4, 6-6: ภาพหน้าจอ Deneb Editor จริง (Power BI 2.157.1354.0 + Deneb 2.0.0.0, ผู้ใช้จับ 26 ก.ย. 2026) ผู้เขียนปิดชื่อบัญชีมุมขวาบนด้วยสี่เหลี่ยมสีเดียวกับแถบชื่อ (ถ้าเห็นชื่อบัญชีให้ระบุ Mandatory)
- 6-5: Power Query Editor จริง (ไม่มีชื่อบัญชีในภาพ)
- **6-3: ภาพที่ผู้เขียนตัดและขยายจาก 6-1 และ 6-2** (ไม่ใช่ภาพหน้าจอใหม่) caption และกล่องต้นบทระบุแล้ว ตรวจว่าขัดกับกฎภาพใน PROJECT_PLAN หรือไม่ ("ห้ามวาดจากการคาดเดา UI" — ภาพนี้มาจากพิกเซลจริง)

หลักฐาน: `qa/scripts/run-ch06-claims-check.mjs` (31 ผ่าน) ตรวจว่าบล็อก JSON ในบท (3 บล็อก `<!-- excerpt: ... -->`) ตรงกับ layer ในไฟล์ Step จริง, ลำดับ layer ต่อ Step, ความสัมพันธ์สะสม S01=CH05-S05+ชั้นพื้นที่ / S02 / S03, ค่าสี/ความหนา/strokeDash, ผลต่างรายเดือนและรายชื่อเดือน Good/Bad ทั้งสองทิศทางคำนวณซ้ำจาก CSV, โครงสร้างช่วงใน `workshop-plotdata.json`, ภาพมีจริง เลขภาพ 6-1..6-6 ต่อเนื่อง

## การตัดสินใจ/ข้อสมมติที่ผู้เขียนแจ้งในบท — ขอให้ตรวจ overclaim

1. **Step 2 เส้นขอบประมองแทบไม่เห็น** ภาพ 6-2 เกือบเหมือน 6-1 (ต่างกัน 879 พิกเซลในพื้นที่ Preview) เพราะเส้นขอบเดินตามขอบพื้นที่ซึ่งถูกเส้น Actual/Reference บังไว้ บทเขียนตรงๆ ว่ามองยากและเพิ่มภาพขยาย 6-3 ตรวจว่าการเล่าแบบนี้ยอมรับได้ หรือควรเป็น Mandatory (เช่นให้แก้ spec ให้เห็นชัด) — ผู้เขียนเลือกไม่แก้ spec เพราะต้องตรงกับ final spec
2. **`detail: Segment_ID`** ผู้เขียนอธิบายว่าแยกรูปพื้นที่ต่อช่วง แต่ระบุว่าไม่ได้ลองถอดออก ตรวจกับเอกสาร Vega-Lite
3. **กรณีผลต่างเป็นศูนย์ (มิ.ย.)** Connector ยาวศูนย์ มองไม่เห็น สีตก Bad ตามเงื่อนไข strict; พื้นที่ พ.ค.–มิ.ย. Good, มิ.ย.–ก.ค. Bad (ตรวจจากภาพ 6-1/6-4 และ `workshop-plotdata.json`)
4. **Step 4** ผู้ใช้ทำ Replace Values ใน query Settings จริง (ภาพ 6-5) และเห็นสีสลับ (ภาพ 6-6) แต่ (ก) ไม่มีภาพเส้นประ Bad ใน Lower is Good ผู้เขียนอนุมานจาก filter และระบุในบท (ข) ข้อความ "ค่าว่าง/ไม่ตรง allow-list ตกเป็น Higher is Good" อ่านจากโค้ด query ยังไม่ทดสอบบน Power Query จริง (ระบุว่าไปบทที่ 9) (ค) วิธีคืนค่าด้วยการกด ✕ หน้า step Replaced Value ผู้เขียนเห็นปุ่มนี้ในภาพ 6-5 แต่ยังไม่มีภาพยืนยันหลังคืนค่า
5. แบบฝึกหัด Step 1–3 เป็นการแก้ property ง่ายๆ ยังไม่ได้ลองใน Deneb จริง (บทระบุใน Step 2, 3; Step 1 คือ opacity)
6. ข้อความ "ถ้าต้องการให้เส้นกับพื้นที่ตรงกันเป๊ะให้ลบ `interpolate: monotone`" ผู้เขียนไม่ได้ลองใน Deneb จริง (ระบุในบท) ตรวจว่าเหมาะจะอยู่ในบทหรือไม่ เพราะขัดกับคำตัดสินผู้ใช้ให้คงเส้นโค้ง (ผู้ใช้ตัดสินให้คงไว้ ผู้เขียนแค่บอกทางเลือก)
7. ยังไม่มีภาพ Report view (ขนาด Visual จริง) ของบทนี้ ตรวจว่ายอมรับได้หรือควรเป็น Mandatory

## ขอให้ตรวจ

1. ความถูกต้องของข้อเท็จจริงเทียบไฟล์ Step/ภาพ/Design Plan: ชื่อ layer, สี `#0F766E`/`#B45309`, opacity, strokeDash `[3,2]` เทียบ `[5,3]` ของ Reference, ความหนา 3/2, ลำดับ layer, นิยาม Good/Bad ต่อ `Business_Type`, ตารางผลต่างและรายชื่อเดือน, จำนวน Connector (11 เส้น: เขียว 6 ส้ม 5), ตัวเลข 9 ช่วงตัดกัน + 2 ช่วงแตะ
2. มาตรฐาน Step 12 หัวข้อครบทุก Step (รวม Step 4 ที่ไม่มี JSON ใหม่); ขั้นตอนทำตามได้จริง ตรวจอ่านคำบรรยายภาพเทียบกับภาพจริง (เช่น 6-1 ม.ค.–ก.พ. เขียวแล้วส้ม, 6-4 Connector แต่ละเดือน, 6-6 สีสลับ)
3. เลข "ภาพ 6-x" ทุกจุดอ้างถูก; ชื่อไฟล์ภาพ `CH06-S0x-*` สม่ำเสมอ (ภาพ 6-5/6-6 ใช้คำนำหน้า `CH06-S04`)
4. กล่อง Known limitation M-11 ตรงกับ PROJECT_PLAN หัวข้อ 9 และบทที่ 1/5 (บทนี้เป็นบทแรกที่เห็นจริง)
5. overclaim / คำสอนที่ผู้อ่านทำตามแล้วอาจติด; ความเสี่ยงที่ผู้อ่านลืมคืนค่า `Higher is Good` (มีจุดตรวจใน Step 4 และท้ายบท); ภาษาไทยอ่านง่าย ศัพท์เทคนิคสม่ำเสมอ
6. ความสอดคล้องกับบทที่ 4 (Run_Sign, Segment_ID, การ stamp `Business_Type`) และบทที่ 5

ตอบรูปแบบเดิม: Verdict (PASS/REVISE/BLOCKED), Mandatory findings (ตาราง มี ID ตำแหน่ง ข้อค้นพบ สิ่งที่ต้องแก้), Optional improvements, คำถามที่ต้องรอหลักฐานจากผู้ใช้ — ภาษาไทย
