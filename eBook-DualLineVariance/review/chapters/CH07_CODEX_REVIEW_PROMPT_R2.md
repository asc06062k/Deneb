# Phase 3 — บทที่ 7 review request รอบ 2 (สำหรับ Codex CLI)

รอบ 1 (`review/chapters/CH07_CODEX_VERDICT.md`) ได้ `REVISE` ด้วย Mandatory เดียว: **CH07-M01** ไม่มีหมายเหตุส่งต่อ "การจัดการ Category ยาว" ไปบทที่ 9

การแก้ใน [`manuscript/chapter-07.md`](../../manuscript/chapter-07.md) (ไม่เปลี่ยน spec หรือภาพ):
1. **CH07-M01** เพิ่มในหัวข้อ "ข้อจำกัดที่ต้องรู้ (Group A)" ของ Step 2 ว่าบทนี้ใช้ชื่อเดือนสั้นเท่านั้น Category ชื่อยาวยังไม่ทดสอบ ไปทดสอบในบทที่ 9 (พร้อมชี้ว่าป้ายแกน X มี `labelLimit` แล้วจากบทที่ 5) และเพิ่มบรรทัดในสรุปบท
2. Optional: caption ภาพ 7-4 ระบุเครื่องหมายลบ `−` ยาวกว่ายัติภังค์ `-` และเปลี่ยนประโยคเรื่อง "ชั้นอื่นไม่มี Tooltip" เป็น "ตามโครงสร้าง spec จึงคาดว่า..."
3. Optional เรื่องแบบฝึกหัดที่ยังไม่ได้ลอง: คงไว้ตามเดิม (บทเปิดเผยแล้ว)

`qa/scripts/run-ch07-claims-check.mjs` ยังผ่าน 34/34 ขอให้ตรวจว่า (ก) CH07-M01 ปิดแล้วและไม่มีข้อความใหม่ที่ overclaim (เช่นการอ้าง `labelLimit` ถูกต้องตรงกับ spec/บทที่ 5) (ข) ไม่มี Mandatory ใหม่ ตอบรูปแบบเดิม ภาษาไทย
