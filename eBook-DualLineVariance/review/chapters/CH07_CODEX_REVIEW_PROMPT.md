# Phase 3 — บทที่ 7 review request (สำหรับ Codex CLI)

คุณคือผู้รีวิวอิสระของ eBook สอนสร้าง Dual-Line Variance Chart ด้วย Deneb บทที่ 1–6 `PASS` แล้ว บทนี้คือ **บทที่ 7 Data Label และ Tooltip**: [`manuscript/chapter-07.md`](../../manuscript/chapter-07.md) มี 2 Step ปฏิบัติ (มาตรฐาน 12 หัวข้อ) ใช้ไฟล์เต็ม `specs/steps/CH07-S01-tooltip.vl.json` และ `CH07-S02-data-labels.vl.json` (สร้างจาก final spec ด้วย generator ผ่านชุดทดสอบ Phase 2 แล้ว)

Source of truth: `PROJECT_PLAN.md` (บทที่ 7, มาตรฐาน Step, กฎภาพ, Group A/B), `specs/steps/*`, `specs/dual-line-variance-final.vl.json`, `review/PHASE1_DESIGN_PLAN.md` (Tooltip, Data label, responsive), `qa/PHASE2_STATIC_TEST_LOG.md` (ผลตรวจ Variance tooltip fields), `manuscript/chapter-03.md` (หัวข้อ 3.7 transform), `manuscript/chapter-05.md`, `manuscript/chapter-06.md`

ภาพ: `images/chapter-07/` 8 ภาพ
- 7-1 ถึง 7-7 ภาพหน้าจอจริง (Power BI 2.157.1354.0 + Deneb 2.0.0.0 ผู้ใช้จับ 26 ก.ย. 2026) ผู้เขียนปิดชื่อบัญชีมุมขวาบนด้วยสี่เหลี่ยมสีเดียวกับแถบชื่อ (ถ้าเห็นชื่อบัญชีให้ระบุ Mandatory) ภาพ 7-3, 7-4, 7-6, 7-7 ถ่ายใน Report view ซูม 79% ค่าขนาด Visual จริงอ่านจากแผง Format ในภาพ 7-6 (Height 450, Width 800) และ 7-7 (Height 180, Width 280)
- **7-8 ภาพที่ผู้เขียนตัดและขยายจาก 7-7** (ไม่ใช่ภาพหน้าจอใหม่ ระบุในกล่องต้นบทและ caption)
- ภาพ 7-5 (ผู้ใช้ชี้จุดสีส้ม Reference แล้วไม่มี Tooltip) **ไม่มีภาพ** บทบันทึกเป็น "ผู้ใช้รายงาน"

หลักฐาน: `qa/scripts/run-ch07-claims-check.mjs` (34 ผ่าน) ตรวจว่าบล็อก JSON ในบท (3 บล็อก) ตรงกับไฟล์ Step จริง, S01 เปลี่ยนเฉพาะ `point_actual_hit_target`, S02 เพิ่มสองชั้นป้ายท้ายอาร์เรย์, ค่า Tooltip (ผลต่าง/เปอร์เซ็นต์ ม.ค. ก.พ. มี.ค. พ.ค. ธ.ค.) คำนวณซ้ำจาก CSV, ตาราง `labelStep` ตามความกว้าง และเดือนที่มีป้ายที่แต่ละ step คำนวณซ้ำจากสูตร, ภาพมีจริง เลขภาพ 7-1..7-8 ต่อเนื่อง

## การตัดสินใจ/ข้อสมมติที่ผู้เขียนแจ้งในบท — ขอให้ตรวจ overclaim

1. **ผู้เขียนประมาณผิดก่อนถ่ายภาพ** ผู้เขียนประมาณว่า Visual 280×180 จะได้ `labelStep` = 2 แต่ภาพจริง (7-7/7-8) แสดงเดือน 1, 4, 7, 10, 12 = `labelStep` 3 บทเขียนตามภาพจริงและบอกตรงๆ ว่าประมาณผิด พร้อมอธิบายว่าความกว้างพื้นที่กราฟอยู่ในช่วง 130.4 ถึงน้อยกว่า 195.6 พิกเซล (ไม่ได้อ่านค่า `width` จาก Signals) ตรวจว่าการเล่านี้ยอมรับได้ และตรวจสูตร/ตารางช่วงความกว้าง
2. **ค่า 0.62 และ 14 ในสูตร `labelStep`** บทบอกว่าเป็นค่าประมาณของผู้เขียนเล่ม ไม่ได้มาจากเอกสาร Vega-Lite ตรวจกับ Design Plan/final spec ว่าถ้อยคำถูกต้อง
3. **Power BI tooltip handler** บทเขียนว่าเปิดอยู่ในภาพ 7-1 แต่ไม่ทราบว่าเป็นค่าตั้งต้นหรือไม่ และไม่ได้ทดลองปิด ตรวจว่าไม่ overclaim
4. **การป้องกันหารศูนย์ (`N/A (เป้าหมาย = 0)`)** ข้อมูล Workshop ไม่มีกรณีนี้ ผู้เขียนไม่ได้เห็นบนหน้าจอ (บอกในบท ส่งไปบทที่ 9) และไม่ยืนยันรูปแบบ format ของผลต่าง 0 (มิ.ย.)
5. **ชี้จุดสีส้มแล้วไม่มี Tooltip** เป็นคำบอกของผู้ใช้ ไม่มีภาพ และการอ้างว่าชั้นอื่น (พื้นที่ เส้น Connector) ไม่มี Tooltip อ่านจาก spec ไม่ได้ทดสอบทุกชั้น
6. **เครื่องหมายลบเป็นขีดยาว** บทระบุเพียงว่า "เครื่องหมายลบเป็นขีดยาว" (จากภาพ 7-4) ตรวจว่าเหมาะสม
7. ข้อจำกัด Group A: บทระบุชัดว่าการลดป้าย "ลดการชน ไม่รับประกัน" และป้ายชื่อเดือนแกน X เป็นคำสัญญาที่แข็งกว่า (Group B) ตรวจกับ PROJECT_PLAN
8. แผนใน PROJECT_PLAN บทที่ 7 มี "การจัดการ Category ยาว" ผู้เขียนไม่ได้ใส่ในบทนี้ (บทที่ 9 มี Category ชื่อยาวและ Category จำนวนมากอยู่แล้ว) ตรวจว่ายอมรับได้ หรือควรเป็น Mandatory ให้เพิ่มหมายเหตุอ้างบทที่ 9
9. แบบฝึกหัดทั้งสอง Step ยังไม่ได้ลองใน Deneb จริง (บทระบุ)

## ขอให้ตรวจ

1. ความถูกต้องของข้อเท็จจริงเทียบไฟล์ Step/ภาพ/Design Plan: ชื่อ layer, สี `#2563EB`/`#B45309`, `dy` ±12, `fontSize` 10, สูตร `calculate` ทั้งสาม, format `+,.0f`/`+.1%`/`,.0f`, ตัวเลข Tooltip ของ พ.ค. และ ก.พ. เทียบภาพ 7-3/7-4, ตาราง `labelStep`, เดือนที่มีป้ายทั้งสองขนาด, คำอธิบาย `joinaggregate` และ `width`
2. มาตรฐาน Step 12 หัวข้อครบทุก Step; ขั้นตอนทำตามได้จริง (หัวข้อ 7.1: Format > Visual > Size and style > Size and position) อ่านคำบรรยายภาพเทียบกับภาพจริง (7-1 ถึง 7-8)
3. เลข "ภาพ 7-x" ทุกจุดอ้างถูก; ชื่อไฟล์ `CH07-S0x-*` สม่ำเสมอ
4. overclaim / คำสอนที่ผู้อ่านทำตามแล้วอาจติด (เช่น ลืมคืนขนาด Visual, ทดสอบ Tooltip ใน Editor แทนหน้ารายงาน); ภาษาไทยอ่านง่าย ศัพท์เทคนิคสม่ำเสมอ
5. ความสอดคล้องกับบทที่ 3 (transform), บทที่ 5–6 (ชื่อ layer, `tooltip: null`) และ Design Plan

ตอบรูปแบบเดิม: Verdict (PASS/REVISE/BLOCKED), Mandatory findings (ตาราง มี ID ตำแหน่ง ข้อค้นพบ สิ่งที่ต้องแก้), Optional improvements, คำถามที่ต้องรอหลักฐานจากผู้ใช้ — ภาษาไทย
